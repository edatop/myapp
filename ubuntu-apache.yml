- hosts: ubuntu
  tasks:
  - name: Install the latest version of Apache
    apt:
      name: apache2, unzip
      state: latest

  - name: Make sure a service is running
    systemd:
      name: apache2
      state: started
      enabled: yes

  - name: Unarchive a file that needs to be downloaded 
    unarchive:
      src: https://www.free-css.com/assets/files/free-css-templates/download/page287/cakezone.zip
      dest: /var/www/
      remote_src: yes